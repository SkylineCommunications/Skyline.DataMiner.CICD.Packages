name: DataMiner CICD NuGet Solution

on:
  push:
    branches:
      - '**'
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"
      - "[0-9]+.[0-9]+.[0-9]+-**"

  workflow_dispatch:

jobs:

  CICD:
    uses: SkylineCommunications/_ReusableWorkflows/.github/workflows/NuGet Solution Master Workflow.yml@MasterWorkflow
    with:
      sonarCloudProjectName: SkylineCommunications_Skyline.DataMiner.CICD.Packages
      solutionName: Skyline.DataMiner.CICD.Packages.sln
    secrets:
      nugetApiKey: ${{ secrets.NUGETAPIKEY }}
