# YLE Media Services

This solution contains all automation script used in the YLE Media Services project.

## Script Library

There is 1 script library project that contains shared code to be used in all other scripts.

## Actions

On every push, automatic quality checks (unit tests, SonarCloud,...) are being triggered.

Seperate actions are available to easily push the script package to the internal setup or to the YLE staging setup.